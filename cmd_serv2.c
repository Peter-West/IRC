#include "irc_server.h"
/*
void		add_to_list(t_list **list, void *data)
{
	t_list		*tmp;
	t_list		*new_link;

	new_link = (t_list*)malloc(sizeof(t_list));
	new_link->data = data;
	new_link->next = NULL;
	if (!*list)
		*list = new_link;
	else
	{
		tmp = *list;
		while (tmp->next)
			tmp = tmp->next;
		tmp->next = new_link;
	}
}*/